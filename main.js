// let message = prompt("Enter the name: ");

// while (message !== "exit") {
//     console.log(message);

//     message = prompt("Enter the name: ");
// }

for (let message = prompt("Enter the name: "); message !== "exit"; message = prompt("Enter the name: ")) {
    console.log(message);
}